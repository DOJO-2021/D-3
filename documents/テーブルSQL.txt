//IDPWテーブル
create table IDPW(
	user_id varchar(20) primary key,
	password varchar(20) not null
);

//Userテーブル
create table User(
	user_id varchar(20) primary key,
	name varchar(30) not null,
	nickname varchar(30) not null,
	birthplace varchar(30) not null,
	company varchar(30) not null,
	birth varchar(20),
	school varchar(30),
	hobby varchar(255),
	intro varchar(511)
);

//Roomテーブル
create table room (
	r_id int auto_increment,
	r_name varchar (70) primary key,
	r_comment varchar (255)  not null,
	release smallint  not null,
	user_id varchar (20)  not null
);

//Chatテーブル
Create table Chat(
message_id varchar(20) auto_increment,
user_id varchar(20) not null,
r_id int,
message varchar(255) not null
);

//Memberテーブル
Create table Member(
user_id varchar(20) not null,
r_id int 
);

//Followテーブル
Create table Follow(
user_id varchar(20),
f_user_id varchar(20)
);


//Reactionテーブル
create table Reaction (
user_id varchar (20),
message_id varchar (20)
);





INSERT INTO IDPW VALUES('D3', 'shiro');

INSERT INTO User VALUES('D3', '白クマ太郎','白クマ','北極3-21-4','株式会社温暖化防止','1998-04-22','北極北大学','魚のつかみ取り','よろしくお願いします');

INSERT INTO Room VALUES(null, '猫好きの会','猫好きな人誰でもどうぞ',1,'D3');

INSERT INTO Chat VALUES(null, 'D3',1,'はじめまして！');

INSERT INTO Member VALUES('D3', 1);

INSERT INTO Follow VALUES('D3', 'D3-21');

INSERT INTO Reaction VALUES('D3',1);
